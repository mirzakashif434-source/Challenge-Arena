plugins { id 'com.android.application' }

android { namespace 'com.challengearena.app'; compileSdk 36
    defaultConfig { applicationId 'com.challengearena.app'; minSdk 23; targetSdk 36; versionCode 1; versionName '1.0' }
    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}
